users:
  admin:
    password: "<REPLACE-WITH-SHA256-HASH>"
    name: "Admin"
    email: "admin@recurrsens.eu"
# Generate your password hash with:
# echo -n "yourpassword" | shasum -a 256 | awk '{print $1}'
