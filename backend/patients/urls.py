"""
URL patterns for the patients app.
Stub file — will be populated in Phase 2 with DRF viewsets.
"""
from django.urls import path

app_name = 'patients'

urlpatterns = [
    # Phase 2: DRF viewsets will be registered here
]
